---
// Floating ring navigator — the trigger button plus the full-screen ring menu.
// Sliced verbatim from the original static pages (src/partials/_ringnav.html); it is
// wired up by /assets/js/nav.js, which BaseLayout loads on every page.
import ringNav from '../partials/_ringnav.html?raw';
---
<Fragment set:html={ringNav} />
